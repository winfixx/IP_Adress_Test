# ѕрограмма дл€ анализа журнала доступа по IP-адресам

ƒан  файл, содержащий список IP-адресов протокола IPv4 из журнала доступа. Ќа каждой строке записан адрес и врем€, в которое с него пришЄл запрос.

Ќеобходимо разработать консольное приложение, которое  выводит в файл список IP-адресов из файла журнала, вход€щих в указанный диапазон с количеством обращений с этого адреса в указанный интервал времени. јдрес и врем€ доступа разделено двоеточием. 
ƒата в журнале доступа записана в формате: yyyy-MM-dd HH:mm:ss

¬се параметры передаютс€ приложению через параметры командной строки:

- --file-log Ч путь к файлу с логами
- --file-output Ч путь к файлу с результатом
- --address-start Ч  нижн€€ граница диапазона адресов, необ€зательный параметр, по умолчанию обрабатываютс€ все адреса
- --address-mask Ч маска подсети, задающа€ верхнюю границу диапазона дес€тичное число. Ќеоб€зательный параметр. ¬ случае, если он не указан, обрабатываютс€ все адреса, начина€ с нижней границы диапазона. ѕараметр нельз€ использовать, если не задан address-start
- --time-start Ч  нижн€€ граница временного интервала
- --time-end Ч верхн€€ граница временного интервала.

ƒаты в параметрах задаютс€ в формате dd.MM.yyyy

## ѕримеры команд:

--file-log ./input.txt --file-output ./output.txt --address-start 0.0.0.0 --address-mask 32 --time-start 01.02.2000 --time-end 10.04.2024

--file-log ./input.txt --file-output ./output.txt --address-start 192.168.1.112 --address-mask 32 --time-start 01.02.2005 --time-end 10.04.2024

--file-log ./input.txt --file-output ./output.txt --address-start 192.168.1.124 --address-mask 32 --time-start 01.02.2015 --time-end 10.04.2024
